import 'package:flutter/material.dart';

class Command_page extends StatelessWidget {
  const Command_page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
